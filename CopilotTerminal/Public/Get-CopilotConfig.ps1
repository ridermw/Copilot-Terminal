function Get-CopilotConfig {
    [CmdletBinding()]
    param(
        [switch]$Raw
    )
    # TODO: Implement - loads default-config.json, overlays user config from
    # ~/.copilot-terminal/config.json, returns merged object (or raw JSON with -Raw)
    Write-Warning "Get-CopilotConfig is not yet implemented."
}
